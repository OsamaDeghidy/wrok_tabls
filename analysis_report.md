# تقرير تحليل مشروع Worktable System

## نظرة عامة
المشروع عبارة عن نظام إدارة موارد بشرية وجداول تشغيلية (Worktable System) مبني باستخدام Django. يحتوي النظام على عدة تطبيقات لإدارة المستخدمين، الفروع، الجداول، الإجازات، الموافقات، والمخالفات.

## هيكلية المشروع
المشروع يتبع هيكلية Django القياسية مع تقسيم الوظائف إلى تطبيقات (Apps) منفصلة داخل مجلد `apps`.

### التطبيقات الرئيسية:
1.  **users**: إدارة المستخدمين والملفات الشخصية والصلاحيات.
2.  **branches**: إدارة الفروع والشفتات.
3.  **schedules**: إدارة الجداول التشغيلية وتوزيع الموظفين.
4.  **leaves**: إدارة طلبات الإجازات وأرصدتها.
5.  **approvals**: نظام موافقات متعدد الخطوات.
6.  **violations**: إدارة المخالفات (مكتمل كنموذج، يحتاج لـ Views).
7.  **employees**: (فارغ تقريباً - يبدو أن `users.UserProfile` يغطي وظيفته).
8.  **reports**: (فارغ).
9.  **integrations**: (فارغ).
10. **dashboard**: (يحتوي على View بسيط).

## الأخطاء والملاحظات التقنية

### 1. أخطاء برمجية ومنطقية (Critical & Major)

*   **سباق البيانات (Race Condition) في توليد الرقم الوظيفي**:
    *   **الموقع**: `apps/users/models.py` في دالة `save`.
    *   **الوصف**: يتم حساب `job_id` الجديد بناءً على آخر رقم موجود (`last_profile`). في حالة قيام مستخدمين بالتسجيل في نفس اللحظة، قد يحصلان على نفس الرقم الوظيفي.
    *   **الحل المقترح**: استخدام `transaction.atomic` مع `select_for_update` أو استخدام Sequence خاص بقاعدة البيانات.

*   **مشاكل التوقيت والمناطق الزمنية**:
    *   **الموقع**: `apps/branches/models.py` (دالة `get_duration`) و `apps/schedules/models.py` (دالة `save`).
    *   **الوصف**: يتم استخدام `datetime.today()` و `datetime.combine` لحساب المدة. هذا يعتمد على توقيت السيرفر المحلي وقد لا يتوافق مع `TIME_ZONE` المحدد في الإعدادات ('Asia/Riyadh')، مما قد يسبب أخطاء في حساب الساعات خاصة عند التعامل مع تواريخ مختلفة.
    *   **الحل المقترح**: استخدام `django.utils.timezone.now()` و `timezone.localtime`.

*   **منطق "Hardcoded"**:
    *   **الموقع**: `apps/users/models.py`.
    *   **الوصف**: يوجد شرط `if self.user.username.startswith('98979')` لتحديد ساعات العمل. هذا منطق هش ويجب تجنبه.
    *   **الحل المقترح**: إضافة حقل في النموذج يحدد نوع الموظف (داخلي/خارجي) أو مجموعة (Group) بدلاً من الاعتماد على بادئة اسم المستخدم.

*   **تجاوز حماية CSRF**:
    *   **الموقع**: `apps/schedules/api_views.py` و `apps/users/views.py`.
    *   **الوصف**: استخدام `@csrf_exempt` على نطاق واسع في الـ API Views. على الرغم من استخدام `SessionAuthentication`، فإن هذا قد يعرض المستخدمين لهجمات CSRF إذا تم استخدام الـ API من المتصفح مباشرة.
    *   **الحل المقترح**: إزالة `csrf_exempt` واستخدام توكن CSRF في طلبات الـ AJAX، أو استخدام `TokenAuthentication` فقط للـ API.

### 2. جودة الكود والصيانة (Code Quality & Maintainability)

*   **غياب Serializers**:
    *   **الموقع**: جميع ملفات الـ Views و API Views.
    *   **الوصف**: يتم تحويل البيانات إلى JSON يدوياً (Manual Serialization). هذا يجعل الكود طويلاً، صعب الصيانة، وعرضة للأخطاء عند تغيير النماذج.
    *   **الحل المقترح**: استخدام `Django REST Framework Serializers`.

*   **تكرار الكود (DRY Violation)**:
    *   **الموقع**: التحقق من الصلاحيات يتكرر في كل View تقريباً.
    *   **الحل المقترح**: استخدام `Permissions Classes` في DRF أو `Decorators` مخصصة أو `Mixins` لتوحيد منطق الصلاحيات.

*   **تطبيقات فارغة**:
    *   تطبيقات `employees`, `reports`, `integrations` موجودة ولكنها فارغة أو غير مستخدمة فعلياً. يجب إما تفعيلها أو إزالتها لتنظيف المشروع.

*   **استعلامات قاعدة البيانات (N+1 Problem)**:
    *   **الموقع**: `apps/branches/views.py` في `branch_detail_view`.
    *   **الوصف**: يتم جلب `shifts` و `employees` بشكل منفصل وقد يتم عمل استعلام لكل فرع في القوائم إذا لم يتم استخدام `prefetch_related` بشكل صحيح في جميع الأماكن.

### 3. التحسينات المقترحة (Recommendations)

1.  **توحيد التعامل مع الـ API**: الانتقال الكامل إلى Django REST Framework (DRF) لجميع نقاط النهاية (Endpoints) التي ترجع JSON.
2.  **تحسين إدارة الصلاحيات**: بدلاً من فحص `request.user.profile.role` في كل دالة، يفضل استخدام نظام الصلاحيات (Permissions) الخاص بـ Django أو DRF.
3.  **إكمال التطبيقات الناقصة**: البدء بتنفيذ `violations` views و `reports` لأهميتها في النظام.
4.  **اختبارات الوحدة (Unit Tests)**: ملفات `tests.py` فارغة تقريباً. يجب إضافة اختبارات للسيناريوهات الحرجة (مثل حساب الساعات، تداخل الإجازات، وتوليد الرقم الوظيفي).

## الخلاصة
المشروع يحتوي على أساس جيد وبنية تحتية قوية، ولكن يحتاج إلى مراجعة لبعض المنطق البرمجي "Hardcoded" ومعالجة مشاكل التزامن والوقت، بالإضافة إلى تحسين أسلوب كتابة الـ API لضمان قابلية التوسع والصيانة مستقبلاً.
